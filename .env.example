# ==============================================================================
# CLOUDFLARE TUNNEL CONFIGURATION
# ==============================================================================
# Cloudflare Tunnel token for HTTPS access from internet
# Get your token from: https://one.dash.cloudflare.com/
# Navigate to: Access > Tunnels > Create/Configure Tunnel
CLOUDFLARE_TUNNEL_TOKEN=

# ==============================================================================
# VITE ALLOWED HOSTS
# ==============================================================================
# Comma-separated list of hostnames allowed by Vite dev server
# This should match your Cloudflare tunnel hostname
VITE_ALLOWED_HOSTS=

# ==============================================================================
# API CONFIGURATION
# ==============================================================================
# Base URL for IdleMMO API
# In development: defaults to /api/v1 (proxied by Vite to https://api.idle-mmo.com)
# In production: set to your proxy endpoint (e.g., /api/v1 if using nginx proxy)
#                or https://api.idle-mmo.com/v1 if CORS is handled server-side
# VITE_API_BASE_URL=/api/v1

# ==============================================================================
# STATIC MODE (GitHub Pages deployment)
# ==============================================================================
# When set to "true", all edit buttons, settings panels, API key UI,
# and market refresh buttons are removed from the built output.
# This creates a read-only static deployment suitable for GitHub Pages.
# VITE_STATIC_MODE=true
