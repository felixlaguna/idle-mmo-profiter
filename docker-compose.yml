services:
  # Vite dev server
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: idle_mmo_profiter_app
    command: npm run dev -- --host 0.0.0.0
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "5173:5173"
    environment:
      - NODE_ENV=development
    stdin_open: true
    tty: true

  # Cloudflare Tunnel (for HTTPS access)
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: idle_mmo_profiter_cloudflared
    command: tunnel run
    environment:
      TUNNEL_TOKEN: ${CLOUDFLARE_TUNNEL_TOKEN}
    restart: unless-stopped
    depends_on:
      - app
